syntax = "proto3";

message ConnectionMessage {
  int32 person_id = 1;
  datetime start_date = 2;
  datetime end_date = 3;
  int32 distance = 4;
}

message ConnectionResult {
	# location
	# person
	# need to check what is the API format returned
}

service ConnectionService {
    rpc GetConnections(ItemMessage) returns (ConnectionResult);
}